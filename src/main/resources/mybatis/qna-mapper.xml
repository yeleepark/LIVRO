<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="qna">

<select id="selectList" resultType="QnaDto">
	SELECT QNA_NO, QNA_FLAG,MEMBER_NICKNAME, QNA_TITLE, QNA_CONTENT, QNA_REGDATE
	FROM QNA_BOARD
	ORDER BY QNA_NO DESC
</select>

<select id="selectOne" resultType="QnaDto" parameterType="int">
	SELECT QNA_NO, MEMBER_NICKNAME, QNA_TITLE, QNA_CONTENT, QNA_REGDATE
	FROM QNA_BOARD
	WHERE QNA_NO = #{qna_no}
</select>

<insert id="insert" parameterType="QnaDto">
	INSERT INTO QNA_BOARD(QNA_NO, MEMBER_NICKNAME, QNA_TITLE, QNA_CONTENT, QNA_REGDATE)
	VALUES(QNA_NO_SEQ.NEXTVAL, #{member_nickname}, #{qna_title}, #{qna_content}, SYSDATE)
</insert>

<update id="update" parameterType="QnaDto">
	UPDATE QNA_BOARD
	SET QNA_TITLE = #{qna_title}, 
	QNA_CONTENT = #{qna_content}
	WHERE QNA_NO = #{qna_no}
</update>

<delete id="delete" parameterType="int">
	DELETE 
	FROM QNA_BOARD
	WHERE QNA_NO = #{qna_no}
</delete>
</mapper>
